<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.skripsi.penjadwalan">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Penjadwalan">
        <activity android:name=".activity.Tambah_KuliahPenggantiDosen"></activity>
        <activity android:name=".activity.DetailPeminjaman" />
        <activity android:name=".activity.Peminjaman_Lab" />
        <activity android:name=".activity.Tambah_KuliahPenggantiAsisten" />
        <activity android:name=".activity.Tambah_Pinjam_Lab" />
        <activity android:name=".activity.Menu" />
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.Login" />
        <activity
            android:name=".activity.Splash"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
